# Python dependencies for the volume 3 build image (tipvol3).
#
# Two groups: what the container needs to RENDER the book, and what the
# CHAPTERS import. Keep them separate — the first must stay small, the second
# grows with each new thematic chapter.
#
# Rebuild after changing this file:
#   docker build --build-arg QUARTO_VERSION=1.9.38 \
#     -t tipvol3:quarto-1.9.38 -f docker/linux/Dockerfile .

# --- Rendering toolchain ---
jupyter>=1.0.0
jupyter-cache
jupyterlab-quarto>=0.3.0
marimo
pyyaml>=6.0

# --- Scientific base ---
numpy>=1.24.0
pandas>=2.0.0
matplotlib
requests>=2.31.0

# --- Geospatial stack ---
geopandas
rasterio
rioxarray
shapely
pyproj

# --- SWOT chapter (00-SWOT.qmd) ---
earthaccess
h5netcdf
contextily
folium
tqdm
python-dotenv

# --- Interactive visualisation (hvplot/holoviews stack) ---
holoviews
hvplot
bokeh
geoviews

# Notes on what is deliberately absent:
#  * torch, opencv, scikit-learn, scikit-image, scipy, seaborn, numba, spyndex,
#    supertree, xrscipy, leafmap, mapclassify, localtileserver — volume 1 only.
#  * gradio, openai, groq, ollama, nltk, betterbib, absl-py, titlecase, rich,
#    black, pre-commit, yamllint, jsonschema, pypandoc, pybtex, ghostscript —
#    inherited from the upstream MLSysBook image, unused here.
#  * gdown — only download_data.py used it, and that script fetches volume 1
#    data files.
#  * csrspy — the SWOT chapter uses a patched fork downloaded at runtime from
#    the SWOT-Canada repo, not the PyPI package.
